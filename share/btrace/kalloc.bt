/*	$OpenBSD$	*/

/*
 * kalloc.bt	Trace kernel allocation functions with type-aware arguments.
 *
 * Uses wildcard kprobes to match all kernel functions containing "alloc"
 * and displays probe name with CTF-formatted arguments.
 */
kprobe:*alloc*:entry {
	printf("%s(%s)\n", probe, arg0);
}
