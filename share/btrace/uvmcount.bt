/*	$OpenBSD$	*/

/*
 * uvmcount.bt	Count calls to VM subsystem functions.
 *
 * Uses wildcard kprobes to match all uvm_* kernel functions and
 * prints call counts every 5 seconds.
 */
kprobe:uvm_* {
	@[probe] = count();
}

interval:s:5 {
	print(@);
}
